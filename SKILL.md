---
name: multi-agent-team
description: >-
  Multi-Agent Team Builder（多智能体团队搭建）：把"想搭多 Agent 系统"的意图直接落地成可运行项目。
  内部判定需求形态并选型；从统一角色池（335 个唯一角色 / 23 个领域）复用专家人设，并用不去重的全量清单（608 条）组建领域小组。
  每领域至少 2 名专家：先小组讨论收敛出"选哪个方案及理由"，再团队讨论定"如何实施该方案"，最后落地执行或给出具体示例。
  讨论过程极度精简（只给结论 + 关键理由）。
  触发词：「多Agent」「多Agent团队」「帮我新建一个多Agent团队」。
  Use when the user wants to scaffold or build a multi-agent system, or mentions "多Agent" / "多Agent团队" / "帮我新建一个多Agent团队".
agent_created: true
---

# 多智能体团队搭建（multi-agent-team）

## 用途
把「想搭多 Agent 系统」的意图变成**实际可运行的项目**：判定形态 → 选型（内部判断，**不对外纠结"该用哪个"**）→ 组领域小组 → 两类讨论收敛 → 落地执行或给出具体示例。**选完立刻动手用，禁止只给选型建议就停。**

## 何时触发
- 用户说「多Agent」「多Agent团队」「帮我新建一个多Agent团队」
- 用户想比较 / 选用多智能体框架或角色包
- 用户希望自动产出调研、写作、代码生成、专家协作类流程

## 第一步：判断「需求形态」（选型的决定性一步）
动手前先判断用户要的是哪一类，再决定走哪条路线：

- **形态 A — 要现成专家角色/人设直接干活**：把一堆"专家"塞进主流 AI 编码工具里即插即用。
  → 选 **角色库系列**：英文通用库（按部门组织）或中文角色库（补齐小红书/抖音/微信/飞书/钉钉等中国市场份额色）。这是**角色/人设包**，不是独立运行的框架，依赖编码工具的执行权限干活。
  → **关键判断**：角色库已覆盖各代码框架所演示的**全部角色原型**（产品经理、架构师、工程师、QA、研究员等）。代码框架是"**引擎**"而非角色来源——真要跑协作仍需引擎，角色库只提供"有哪些专家可用"。
- **形态 B — 要在自己 Python 代码里用 SDK 搭多 Agent 应用**：进入下方「能力类型五选一」。
- **形态 C — 要开箱即用的网页/桌面/Docker 应用**：一键编排器（一句话组"一人公司"）或带沙箱的工作台（研究/自动化工作台）。
- **形态 D — 要企业级/国产 Python 框架、与 Java/Python 既有栈集成**：国产 Python 框架（企业级，纯 Python）。

## 第二步：能力类型五选一（仅形态 B）
| 能力类型 | 最适合 |
|----------|--------|
| 轻量多智能体引擎 | 快速组「小团队」跑具体业务任务（轻量、不依赖第三方链路库） |
| 需求转软件框架 | 一句话需求变软件/文档（模拟 AI 软件公司） |
| 研究型框架 | 多 Agent 研究 / 数据合成实验（角色扮演式自主协作） |
| 可编排代理框架 | 复杂拓扑编排（生产级 pipeline） |
| 带沙箱的工作台 | 开箱研究/自动化工作台 + 沙箱隔离（更安全） |

## 执行流程（必须实际落地）

> **核心准则：选完即动手，不输出纯建议。** 判定「需求形态」和选定框架都是技能的**内部判断**，对外只报"我用了什么、跑成了什么"。除非用户明确说「只要建议」「只对比」，否则一律直接搭建可运行项目。触发「新建一个多Agent团队」尤其如此——直接组队并跑 `--dry-run`。

1. **澄清需求**（最多 3 个关键问题，能推断就别问）：任务目标？是否需要代码执行/隔离？用哪家 LLM？
2. **内部选型**：先用「需求形态」判定大类，再在大类内定好具体框架——**只在脑子里完成**，不向用户列对比、不问"你想用哪个"。
3. **组建领域小组**：按「团队协作规则 规则一」，从全量清单招募成员，每领域 ≥2 人。
4. **两类讨论收敛**：按「规则二 / 规则三」，先小组讨论出"选哪个方案"，再团队讨论定"如何实施"。
5. **落地搭建（直接做，不是建议）**：
   - 代码框架：检查工作区 `ai-agent-frameworks/` 是否已有源码可参考；否则建独立项目目录 + `python -m venv .venv`，在虚拟环境内 `pip install` 所选框架，写最小可运行示例（定义 Agent/角色、Task、串联成团队跑通 demo）。
   - 角色包：直接把角色定义导入/生成到用户的编码工具，并提醒执行权限与审查要点。
   - 应用类（编排器/带沙箱工作台）：按官方 README 用 Docker / 本地启动，跑通一个示例任务。
   - 安全默认：密钥用 `.env` + 环境变量，**绝不硬编码**；优先沙箱；不喂敏感数据。
6. **交付说明**：怎么运行、如何替换成用户真实任务、费用与风险提醒。

## Agent 组建铁律：先从池里取，池没有才新建（触发「新建一个多Agent团队」时强制）

组队顺序**必须**如下，不得一上来就凭空写新 Agent：

1. **先查统一角色池**：只查 **`references/统一角色池.md`** 这一个池（四个来源角色库合并去重后的结果：**335 个唯一角色 / 23 个领域**）。引用格式 `统一角色池/<领域>/<slug>`，如 `统一角色池/engineering/engineering-code-reviewer`。**不得**再分别扫各个源库目录。
2. **复用**：命中即直接引用其 `.md` 作为该 Agent 的 `role / goal / backstory`，不重写；每条角色须带 `domain`（领域）与 `source`（池内 slug）两个字段。
3. **池无则新建**：只有池里确实没有的业务角色才新建；`source` 填 `new:<自定 slug>`，保持同一人设结构，且**名称/职责/能力在所属领域内唯一**。
4. **框架原型可作能力模板**：各代码框架的 Agent/Task、Role、角色扮演、可编排 Agent 等也能作为"能力模板"复用，配合角色池人设一起落地。
5. **落点**：最终团队用 `assets/multi-agent-team-demo/`（或工作区 `agent-team-demo/`）的 `team_config.py` 承载，`TEAM` 每条必须带 `domain` + `source`，先跑 `--dry-run` 验证（含领域小组人数校验）再真实运行。

> 一句话：池是 Agent 池，先从中挑选拼装；池外缺的角色才新建。绝不默认"新建"，默认"复用"。

## 团队协作规则（触发「新建一个多Agent团队」时强制）

### 规则一：每个领域必须 2 名及以上专家，组成领域小组
- 按 `TEAM` 条目的 `domain` 字段分组，**每个领域至少 2 人**，该组即"领域小组"，是讨论与收敛的基本单位。
- 领域可选角色 < 2 时（当前 `research` 与 `integrations` 各仅 1 个角色），从语义最近领域（`academic` / `specialized` / `engineering`）**跨领域补齐**到 2 人。
- 不允许出现"单专家领域"——单个专家无组内辩驳，无法收敛。
- **成员来源**：从 **`references/agent-全量清单.md`**（四库全量、**608 条、不去重**）招募。
  - 小组讨论需要**不同视角**，因此**跨库重复 / 同库多次出现的 agent 一律保留**，以便比较思路差异；
  - 只有最终落地的团队配置才用去重视图（`references/统一角色池.md`，335 个唯一角色）。
- **两个视图的关系**：清单 = 不去重的**讨论**视图；角色池 = 去重后的**组队**视图。讨论用清单，组队用角色池。

### 规则二：两类讨论分工不同（目的不同，输出也不同）

| | **小组讨论**（领域小组内部） | **团队讨论**（跨领域全团队） |
|---|---|---|
| **焦点** | **方案本身** | **方案如何融入当前任务** |
| **要做什么** | 充分比较组内各 agent 的思路与候选方案，辩到收敛 | 讨论落地方式、与既有代码的衔接、职责划分、执行顺序 |
| **产出** | **选哪个方案 + 理由**（一条） | **如何实施该方案**（最终方案 + 执行路径） |

> 两者不可混为一谈：小组负责"做正确的事"，团队负责"把事做正确"。

### 规则三：按顺序收敛（不得跳级）
1. **小组讨论** —— 该领域 2+ 名专家各自提方案，**充分比较彼此思路**，组内辩到收敛。
2. **产出一条小组最佳建议** —— 每领域小组**只输出 1 条**：**选定哪个方案 + 关键理由**；**禁止**把多个分歧方案原样上抛。
3. **团队讨论** —— 各组的那一条建议汇总，讨论**如何落地**：与既有代码的衔接、跨领域职责划分、执行顺序；跨域冲突在此解决。
4. **输出实施方案并执行** —— 收敛出唯一最终方案，**直接执行落地**；无法真实执行时给出**具体示例**（可运行配置 / 代码 / 步骤），**禁止**停在"建议"。

> 一句话：小组答「**选哪个方案及理由**」，团队答「**如何实施该方案**」。禁止跳级，禁止把分歧原样上抛。

## 沟通风格约束：极度精简（讨论过程强制）

- 讨论过程**极度精简**：只给 **结论 + 关键理由**，一段话能删就删，用最短词句把信息传到位（穴居人式压缩表达）。
- **禁止**：长篇解释、背景铺陈、复述他人已说内容、"首先/其次/最后"式套话、客套寒暄。
- **允许**：一句话结论、要点式短句、必要的数字/事实/取舍。
- 收敛记录按"领域小组 → 一条建议 → 团队讨论 → 实施方案"四段输出，每段控制在几句话内。
- **边界**：精简只约束**讨论过程**；最终交付物（配置、代码、文档）必须完整可执行，不得因精简而缺内容。

## 内置：可配置多 Agent 团队脚手架（demo）
技能附带可直接运行的多 Agent 团队模板：`assets/multi-agent-team-demo/`（工作区另有副本 `agent-team-demo/`）。**团队组成完全可配置，不写死角色数。**

- **引擎**：轻量多智能体引擎。引擎**可插拔**：默认引擎 `lightweight`，另内置零依赖的 `mock` 适配器；新增引擎只需在 `engines/` 写一个适配器类并登记。
- **配置**：只改 `team_config.py` 的 `TEAM` 列表。默认按**领域小组**组织（research / engineering / testing 各 2 人），可随意增删领域与角色，但**每领域至少 2 人**。
- **角色素材**：从**统一角色池**按 `<领域>/<slug>` 取（如 `engineering/engineering-code-reviewer`）；每条须带 `domain` + `source`。
- **运行**：`python build_team.py --dry-run` 预览团队（不花钱、不调模型，**无需安装任何引擎**）；`python build_team.py --engine mock` 零依赖演示完整流程；复制 `.env.example` 为 `.env` 填 API Key，再 `python build_team.py --topic "主题"` 真实运行。
- **触发后动作**：按「Agent 组建铁律」查池复用 → 按领域小组（每领域 ≥2 人）增删 `TEAM` → 跑通 `--dry-run`（含领域小组校验）→ 按两类讨论收敛 → 落地。

## 安全须知（每次落地都必须提示）
- 本地代码执行风险：多数代码框架默认本机直接运行 LLM 生成代码；带沙箱的工作台用沙箱更安全。
- **角色包风险**：角色包在你的编码工具里运行，拥有该工具的**同等文件/命令权限**。务必先 Review 角色定义再启用；警惕通过网页/文档注入的恶意指令（prompt injection）。
- API Key 与费用：全部需要 LLM API Key，会真实扣费；输入内容会发往对应服务商。
- 数据外发：prompt 与喂入文件会传给第三方大模型，切勿放入密钥或隐私数据。
- 供应链：可编排代理框架依赖树庞大且历史有争议，务必装在独立虚拟环境并审查。
- 已知漏洞：整理时上述仓库在 GitHub 上均无公开 Security Advisory，来源可信（MIT/Apache-2.0，高星标、近期活跃）。

## 参考与携带说明
- **统一角色池**（组队视图，去重）：`references/统一角色池.md` —— 335 个唯一角色 / 23 个领域。
- **Agent 全量清单**（讨论视图，不去重）：`references/agent-全量清单.md` —— 608 条（agent 583 + 工作流技能 25），重复 454 条 / 227 个路径，逐条标注所属库、路径、职责与重复来源。
- **框架总览 / 选型**：`references/框架总览与选型.md`；**流程图**：`references/团队搭建流程.svg`。
- **可运行脚手架**：`assets/multi-agent-team-demo/`。
- **角色库（已发布，可直接克隆）**：四个来源角色库的角色内容已整理为独立仓库
  **`https://github.com/zhangruilin158/agent-role-libraries`**（583 个角色，按领域组织，原始 MIT/Apache 许可与版权声明保留在 `LICENSES/`）。
  本技能的角色池 / 全量清单即基于它；若本地缺失，克隆该仓库即可（本机到代码托管平台不稳时，先 `git config --global http.sslBackend openssl` + `core.longpaths true` 再克隆）。
- **代码框架源码**：5 类代码框架已下载至工作区 `ai-agent-frameworks/`，仅作参考，未随技能分发。
- 本技能已自带 `agent_created: true`，可直接打包（`SKILL.md` + `assets/` + `references/`）迁移到其他机器或分享。
