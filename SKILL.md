---
name: multi-agent-team
description: >-
  Multi-Agent Team Builder（多智能体团队搭建）：把"想搭多 Agent 系统"的意图直接落地成可运行项目。
  技能内部判定需求形态并选型，从通用角色库复用现成专家人设（库里没有才新建），再用轻量多智能体引擎脚手架跑通团队；
  对外只交付"用了什么、跑成了什么"，不纠结选型、不空给建议。团队组成完全可配置，不限于固定角色。
  触发词：「多Agent」「多Agent团队」「帮我新建一个多Agent团队」。
  Use when the user wants to scaffold or build a multi-agent system, or mentions "多Agent" / "多Agent团队" / "帮我新建一个多Agent团队".
agent_created: true
---

# 多智能体团队搭建（multi-agent-team）

## 用途
把「想搭多 Agent 系统」的意图变成**实际可运行的项目**：判定形态 → 选型（内部判断，**不对外纠结"该用哪个"**）→ 在独立虚拟环境/目录落地 → 产出最小可运行示例或配置。**选完立刻动手用，禁止只给选型建议就停。**

## 何时触发
- 用户说「多Agent」「多Agent团队」「帮我新建一个多Agent团队」
- 用户想比较 / 选用多智能体框架或角色包
- 用户希望自动产出调研、写作、代码生成、专家协作类流程

## 第一步：判断「需求形态」（选型的决定性一步）
动手前先判断用户要的是哪一类，再决定走哪条路线：

- **形态 A — 要现成专家角色/人设直接干活**：用户想把一堆"专家"（前端 wizard、社区运营、质检等）塞进主流 AI 编码工具里即插即用。
  → 选 **通用角色库系列**：英文通用库（几百个专家人设、按部门组织）或中文角色库（补齐小红书/抖音/微信/飞书/钉钉等中国市场份额色）。这是**角色/人设包**，不是独立运行的框架，依赖编码工具的执行权限干活。
  → **关键判断**：通用角色库已覆盖各代码框架所演示的**全部角色原型**（产品经理、架构师、工程师、QA、研究员等），仅缺中国市场份额色（由中文角色库补齐）。这些代码框架是"**引擎**"而非角色来源——真要跑协作仍需引擎，角色库只提供"有哪些专家可用"。
- **形态 B — 要在自己 Python 代码里用 SDK 搭多 Agent 应用**：进入下方「能力类型五选一」。
- **形态 C — 要开箱即用的网页/桌面/Docker 应用**：一键编排器（一句话组"一人公司"）或带沙箱的工作台（研究/自动化工作台）。
- **形态 D — 要企业级/国产 Python 框架、与 Java/Python 既有栈集成**：国产 Python 框架（企业级，纯 Python）。

## 第二步：能力类型五选一（仅形态 B）
| 能力类型 | 最适合 |
|----------|--------|
| 轻量多智能体引擎 | 快速组「小团队」跑具体业务任务（轻量、不依赖第三方链路库） |
| 需求转软件框架 | 一句话需求变软件/文档（模拟 AI 软件公司） |
| 研究型框架 | 多 Agent 研究 / 数据合成实验（角色扮演式自主协作） |
| 可编排代理框架 | 复杂拓扑编排（生产级 pipeline） |
| 带沙箱的工作台 | 开箱研究/自动化工作台 + 沙箱隔离（更安全） |

## 执行流程（必须实际落地）

> **核心准则：选完即动手，不输出纯建议。** 判定「需求形态」和选定框架都是技能的**内部判断**，对外只报"我用了什么、跑成了什么"，**不要**写成"建议你用 X 框架"就停。除非用户明确说「只要建议」「只对比」，否则一律直接搭建可运行项目（脚手架 / 配置 / 启动）。触发「新建一个多Agent团队」尤其如此——直接组队并跑 `--dry-run`。

1. **澄清需求**（最多 3 个关键问题，能推断就别问）：任务目标？是否需要代码执行/隔离？用哪家 LLM（主流大模型服务商 / 本地 / 待定）？
2. **内部选型**：先用「需求形态」判定大类，再在大类内定好具体框架——这一步**只在脑子里完成**，不向用户列对比、不问"你想用哪个"。
3. **落地搭建（直接做，不是建议）**：
   - 代码框架：优先检查工作区 `ai-agent-frameworks/` 是否已下载对应源码可参考；否则建独立项目目录 + `python -m venv .venv`，在虚拟环境内 `pip install` 所选框架，写一个最小可运行示例（定义 Agent/角色、Task、串联成团队跑通 demo）。
   - 角色包（通用角色库）：直接把角色定义导入/生成到用户的编码工具（主流 AI 编码工具等），并提醒其执行权限与审查要点。
   - 应用类（编排器/带沙箱工作台）：按官方 README 用 Docker / 本地启动，跑通一个示例任务。
   - 安全默认：密钥用 `.env` + 环境变量，**绝不硬编码**；优先沙箱；不喂敏感数据。
4. **交付说明**：怎么运行、如何替换成用户真实任务、费用与风险提醒。

## Agent 组建铁律：先从库里取，库没有才新建（触发「新建一个多Agent团队」时强制）

当用户说「新建一个多Agent团队」「多Agent团队」「帮我新建一个多Agent团队」时，组队顺序**必须**如下，不得一上来就凭空写新 Agent：

1. **先查库**：在 `ai-agent-frameworks/通用角色库`（英文，按部门组织几百个专家人设）与 `ai-agent-frameworks/中文角色库`（中国市场份额色：小红书/抖音/微信/飞书/钉钉等）里检索是否有匹配的角色定义（`<角色库>/<部门>/<角色>.md`，如 `engineering/engineering-code-reviewer.md`）。
2. **复用**：命中即直接引用其 `.md` 作为该 Agent 的 `role / goal / backstory`，不重写、不另起炉灶；一个团队可由多个库角色拼成。
3. **库无则新建**：只有库里确实没有的用户特有业务角色，才新建一个 Agent 定义；新建时保持与库一致的人设结构（role/goal/backstory），并记入 `team_config.py` 的 `TEAM`。
4. **框架原型可作能力模板**：各代码框架的 Agent/Task、Role、角色扮演、可编排 Agent 等也能作为"能力模板"复用，配合通用角色库的人设一起落地。
5. **落点**：最终团队用 `assets/multi-agent-team-demo/`（或工作区 `agent-team-demo/`）的 `team_config.py` 承载，`TEAM` 列出实际选中的角色（来自库的 + 必要新建的），先跑 `--dry-run` 验证组队正确再真实运行。

> 一句话：库是 Agent 池，先从中挑选拼装；池外缺的角色才新建。绝不默认"新建"，默认"复用"。

## 内置：可配置多 Agent 团队脚手架（demo）
技能附带一个可直接运行的多 Agent 团队模板：`assets/multi-agent-team-demo/`（工作区另有副本 `agent-team-demo/`）。核心要点：**团队组成完全可配置，不写死角色数**。

- **引擎**：轻量多智能体引擎（模板内含依赖安装说明；或 `pip install -r requirements.txt` 自建虚拟环境）。引擎**可插拔**：默认引擎 `lightweight`，另内置零依赖的 `mock` 适配器，新增引擎只需在 `engines/` 写一个适配器类并登记。
- **配置**：只改 `team_config.py` 里的 `TEAM` 列表——项目需要哪些 Agent 就列哪些。默认示例是研究员 / 工程师 / 审核，但**可随意增删**（产品经理、安全专家、技术作家……项目缺什么角色就加什么）。
- **角色素材**：可直接从通用角色库取人设：`<角色库>/<部门>/<角色>.md`（如 `engineering/engineering-code-reviewer.md`）。
- **运行**：`python build_team.py --dry-run` 预览团队（不花钱、不调模型，**无需安装任何引擎**——直接读 team_config）；`python build_team.py --engine mock` 可零依赖演示完整流程；复制 `.env.example` 为 `.env` 填入 API Key（或兼容服务商），再 `python build_team.py --topic "主题"` 真实运行。
- **触发后动作**：按「Agent 组建铁律」——先到 `ai-agent-frameworks/通用角色库` 与 `中文角色库` 检索匹配角色并复用，库里没有的才新建；据此增删 `TEAM` 后生成可运行团队，并先跑通 `--dry-run`。

## 安全须知（每次落地都必须提示）
- 本地代码执行风险：多数代码框架默认本机直接运行 LLM 生成代码；带沙箱的工作台用沙箱更安全。
- **角色包风险（通用角色库系列特有关键）**：这类角色包在你的编码工具（主流 AI 编码工具等）里运行，拥有该工具的**同等文件/命令权限**。务必先 Review 角色定义再启用；警惕通过网页/文档注入的恶意指令（prompt injection）。
- API Key 与费用：全部需要 LLM API Key，会真实扣费；输入内容会发往对应服务商。
- 数据外发：prompt 与喂入文件会传给第三方大模型，切勿放入密钥或隐私数据。
- 供应链：可编排代理框架依赖树庞大且历史有争议，务必装在独立虚拟环境并审查；角色包多为 Shell/PowerShell/Python 脚本，启用前人工过一遍。
- 已知漏洞：整理时上述仓库在 GitHub 上均无公开 Security Advisory，来源可信（MIT/Apache-2.0，高星标、近期活跃）。

## 参考与携带说明
- 内置框架总览 / 选型文档：`references/框架总览与选型.md`（原说明文档已内嵌，技能自包含，无需依赖工作区文件）。
- 可运行脚手架：`assets/multi-agent-team-demo/`（开箱即用的多 Agent 团队模板）。
- **角色库 / 框架源码**：通用角色库、中文角色库与 5 个代码框架（轻量多智能体引擎 / 需求转软件框架 / 研究型框架 / 可编排代理框架 / 带沙箱的工作台）已下载至工作区 `ai-agent-frameworks/`，铁律与 demo 直接复用其角色定义。若该目录缺失，触发后用克隆工具获取（本机到代码托管平台不稳时，先 `git config --global http.sslBackend openssl` + `core.longpaths true` 再克隆）。
- 本技能已自带 `agent_created: true`，可直接打包（`SKILL.md` + `assets/` + `references/`）迁移到其他机器或分享。
