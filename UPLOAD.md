# 多智能体团队搭建（multi-agent-team）· 上传简介

> 本文件供上传到 Skill 平台时填写简介/描述使用，可直接复制对应段落。

## 一句话简介（标题下方副标题）
把"想搭多 Agent 系统"直接变成能跑的团队：内部选型、按领域组队、小组讨论选方案、团队讨论定落地，不纠结、不空谈。

## 三种形态（大白话）

技能帮你落地的多 Agent 方案，归纳起来就三种形态，按你要做的事对号入座：

- **形态一 · 搬现成专家（角色库即插即用）**：你想把一堆"专家"（前端 wizard、社区运营、质检、安全专家……）直接塞进 AI 编码工具里干活。技能从角色库（英文通用库，按部门组织；中文角色库，补齐小红书/抖音/微信/飞书/钉钉等中国市场份额色）里挑现成人设，即插即用、不重写。这是"角色包"，不是独立程序，靠编码工具的执行权限干活。
- **形态二 · 代码里自己组队（SDK / 轻量引擎脚手架）**：你想在自己的 Python 项目里用代码定义 Agent、跑多 Agent 协作。技能自带轻量多智能体引擎脚手架——改一个 `TEAM` 列表就能增减角色，先 `--dry-run` 免费预览，配密钥再真实运行。这是真正能跑的代码。
- **形态三 · 开箱应用 / 企业级框架**：你想要现成的网页/桌面/Docker 应用，或能和 Java/Python 既有栈集成的企业级框架。技能引导用一键编排器、带沙箱的研究/自动化工作台，或国产 Python 框架落地，跑通生产级 pipeline。

一句话：形态一是"专家池"（有哪些人可用），形态二/三是"引擎"（怎么把专家真正跑起来），三者可组合——角色库人设可直接喂给代码引擎或应用。

## 功能亮点

- **选完直接用**：触发后内部判定需求形态并选型，**直接动手搭建可运行项目**，不输出纯选型建议就停。
- **组队不手写**：从**统一角色池**（四库合并去重，**335 个唯一角色 / 23 个领域**）复用现成专家人设，池里没有的才新建。
- **领域小组 + 两级讨论**：每领域至少 2 名专家成组；**小组讨论**比较各 agent 思路、产出"选哪个方案及理由"，**团队讨论**定"如何实施该方案"（落地方式、与既有代码衔接、职责划分、执行顺序），最后落地执行或给出具体示例。
- **讨论成员不去重**：提供**全量 agent 清单**（四库 **608 条**，不去重、标注重复来源）——小组讨论刻意保留重复 agent，以便充分比较不同思路；仅最终团队配置用去重视图。
- **沟通极度精简**：讨论过程只给**结论 + 关键理由**，禁止长篇解释、复述与客套。
- **完全可配置**：自带可插拔引擎脚手架，只改一个 `TEAM` 列表即可增删角色；先 `--dry-run` 免费预览，配密钥再真实运行。
- **安全兜底**：每次落地都提示本地代码执行风险、API 费用、数据外发、密钥管理；工作台类能力支持沙箱隔离。
- **自包含**：内置框架总览/选型文档、角色池、全量清单与可运行脚手架，无需依赖外部文件即可迁移或分享。

## 默认引擎说明

- 脚手架**默认引擎是「轻量多智能体引擎」**（底层即你保留的 crewai，去品牌后内部命名为 `lightweight`），开箱即用，无需自己选型。
- **引擎可插拔**：除默认 `lightweight`，另内置一个**零依赖的 `mock` 引擎**——不装任何第三方库也能演示完整组队流程（`--engine mock`），或先用 `--dry-run` 免费预览团队（连 mock 都不需要）。
- **换引擎很简单**：改 `team_config.py` 的 `ENGINE`、用命令行 `--engine`，或设环境变量 `TEAM_ENGINE` 任选其一；新增引擎只需在 `engines/` 写一个适配器类并登记到工厂，主流程一行不用改。

## 团队协作规则（亮点）

| 阶段 | 谁参与 | 做什么 | 产出 |
|------|--------|--------|------|
| **小组讨论** | 同领域 ≥2 名专家 | 比较各 agent 思路与候选方案，辩到收敛 | **选哪个方案 + 理由**（每领域仅 1 条） |
| **团队讨论** | 跨领域全团队 | 落地方式、与既有代码衔接、职责划分、执行顺序 | **如何实施该方案** |

若某领域人数不足 2（如 `research`、`integrations`），从相近领域跨领域补齐——单人无组内辩驳，无法收敛。

## 适用场景

- 想快速组一支"小团队"跑调研 / 写作 / 竞品分析等业务任务
- 想用"公司化分工"自动产出代码或文档
- 想做多 Agent 研究 / 数据合成实验
- 需要复杂拓扑编排的生产级 pipeline
- 想要开箱即用的研究 / 自动化工作台（带执行隔离）

## 触发词

- 「多Agent」
- 「多Agent团队」
- 「帮我新建一个多Agent团队」

## 安装方式

把整个 `multi-agent-team/` 文件夹复制到技能目录：
- 用户级：`~/.workbuddy-ai/skills/multi-agent-team/`
- 项目级：`<项目>/.workbuddy-ai/skills/multi-agent-team/`

## 注意事项

- 真实运行会消耗大模型 token、产生费用；先用便宜的入门模型试跑。
- 角色库在你的编码工具中以同等文件/命令权限运行，启用前请 Review 人设、警惕 prompt injection。
- 密钥只在 `.env`，切勿硬编码或提交；输入内容会发往你配置的大模型服务商，勿喂入密钥或隐私数据。
- 角色库与框架源码体积较大，未打包进本技能；若工作区缺失，技能触发后会引导用克隆工具获取。
